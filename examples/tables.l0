-- Tables combine numeric and named keys while keeping one value type.
let board: table<i8> = table {
    [0] = 1,
    [4] = 2,
    winner = 0
}

board[8] = 1
board.winner = 1

print(board[0])
print(board[4])
print(board[8])
print(board.winner)
